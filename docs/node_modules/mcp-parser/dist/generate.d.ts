/**
 * Generate documentation-oriented output from an McpSpec.
 */
import type { McpSpec } from "mcp-schema";
/**
 * Generate an llms.txt file from an McpSpec.
 *
 * The llms.txt format is a concise, LLM-friendly index of a project's
 * documentation — similar to robots.txt but for AI agents.
 *
 * @param spec - The MCP spec to generate from
 * @param baseUrl - Optional base URL for linking to hosted docs
 *
 * @example
 * ```ts
 * import { parse, generateLlmsTxt } from "mcp-parser";
 *
 * const spec = await parse("./mcp.json");
 * const txt = generateLlmsTxt(spec);
 * await writeFile("llms.txt", txt);
 * ```
 */
export declare function generateLlmsTxt(spec: McpSpec, baseUrl?: string): string;
/**
 * Generate an llms-full.txt file — complete documentation as markdown.
 *
 * This is the full reference for LLMs with large context windows.
 *
 * @param spec - The MCP spec to generate from
 */
export declare function generateLlmsFullTxt(spec: McpSpec): string;
/**
 * Generate a complete markdown reference document.
 *
 * @param spec - The MCP spec to generate from
 */
export declare function generateMarkdown(spec: McpSpec): string;
//# sourceMappingURL=generate.d.ts.map