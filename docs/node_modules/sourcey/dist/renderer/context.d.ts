import type { NormalizedSpec } from "../core/types.js";
import type { SiteNavigation } from "../core/navigation.js";
import type { MarkdownPage } from "../core/markdown-loader.js";
import type { NavbarLink, ResolvedTheme } from "../config.js";
/**
 * Options that control rendering behavior.
 */
export interface RenderOptions {
    /** Whether to generate embeddable output (no html/body wrapper) */
    embeddable: boolean;
    /** Base URL for assets (CSS, JS) */
    assetBase: string;
}
/**
 * Full context available to all components during rendering.
 */
export interface RenderContext {
    spec: NormalizedSpec;
    options: RenderOptions;
}
/**
 * What the current page is rendering.
 */
export interface CurrentPage {
    kind: "spec" | "markdown";
    spec?: NormalizedSpec;
    markdown?: MarkdownPage;
}
/**
 * Preact context for passing spec data to deeply nested components.
 */
export declare const SpecContext: import("preact").Context<NormalizedSpec>;
/**
 * Preact context for render options.
 */
export declare const OptionsContext: import("preact").Context<RenderOptions>;
/**
 * Preact context for site-wide navigation.
 */
export declare const NavigationContext: import("preact").Context<SiteNavigation>;
/**
 * Preact context for the current page being rendered.
 */
export declare const PageContext: import("preact").Context<CurrentPage>;
/**
 * Site-wide config available to all layout components.
 */
export interface SiteConfig {
    name: string;
    theme: ResolvedTheme;
    logo?: {
        light?: string;
        dark?: string;
        href?: string;
    };
    favicon?: string;
    repo?: string;
    editBranch?: string;
    editBasePath?: string;
    codeSamples: string[];
    navbar: {
        links: NavbarLink[];
        primary?: {
            type: "button";
            label: string;
            href: string;
        };
    };
    footer: {
        links: NavbarLink[];
    };
    customCSS?: string;
}
export declare const SiteContext: import("preact").Context<SiteConfig>;
//# sourceMappingURL=context.d.ts.map