import { jsx as _jsx, jsxs as _jsxs } from "preact/jsx-runtime";
import { httpStatusText } from "../../utils/http.js";
import { SchemaDatatype } from "../schema/SchemaDatatype.js";
import { ExampleView } from "../schema/ExampleView.js";
import { Markdown } from "../ui/Markdown.js";
import { CopyButton } from "../ui/CopyButton.js";
import { generateExample } from "../../utils/example-generator.js";
import { highlightCode } from "../../utils/highlighter.js";
function statusColorClass(code) {
    if (code.startsWith("2"))
        return "bg-green-100 text-green-800 dark:bg-green-400/20 dark:text-green-300";
    if (code.startsWith("3"))
        return "bg-blue-100 text-blue-800 dark:bg-blue-400/20 dark:text-blue-300";
    if (code.startsWith("4"))
        return "bg-amber-100 text-amber-900 dark:bg-yellow-400/20 dark:text-yellow-300";
    if (code.startsWith("5"))
        return "bg-red-100 text-red-800 dark:bg-red-400/20 dark:text-red-300";
    return "bg-gray-400/20 text-gray-700 dark:text-gray-400";
}
/**
 * Response status list (rendered in the content column).
 */
export function ResponsesCopy({ responses }) {
    if (!responses.length)
        return null;
    return (_jsx("div", { class: "params-list", children: responses.map((r) => (_jsxs("div", { class: "param-item", children: [_jsxs("div", { class: "param-header font-mono text-sm", children: [_jsx("span", { class: `px-1.5 py-0.5 rounded-md text-xs font-bold ${statusColorClass(r.statusCode)}`, children: r.statusCode }), _jsx("span", { class: "font-medium text-[rgb(var(--color-gray-900))] dark:text-[rgb(var(--color-gray-200))]", children: httpStatusText(r.statusCode) }), r.content && (_jsx("span", { class: "param-type", children: renderResponseType(r) }))] }), r.description && (_jsx("div", { class: "param-description", children: _jsx(Markdown, { content: r.description, class: "prose-sm" }) }))] }, r.statusCode))) }));
}
/**
 * Response examples with status code tabs on the code block card.
 * Status code tabs are part of the code block header.
 */
export function ResponsesExamples({ responses }) {
    if (!responses.length)
        return null;
    const examples = responses
        .map((r) => {
        const schema = getResponseSchema(r);
        if (!schema)
            return null;
        const example = schema.example ?? generateExample(schema);
        if (example === undefined)
            return null;
        const json = JSON.stringify(example, null, 2);
        const html = highlightCode(json, "json");
        return { statusCode: r.statusCode, html };
    })
        .filter(Boolean);
    if (!examples.length)
        return null;
    // Single response: use plain ExampleView
    if (examples.length === 1) {
        const r = responses.find((r) => r.statusCode === examples[0].statusCode);
        const schema = getResponseSchema(r);
        return _jsx(ExampleView, { schema: schema, title: `${examples[0].statusCode}` });
    }
    // Multiple responses: tabbed code block with status code tabs in the header
    return (_jsxs("div", { class: "response-tabs code-group not-prose", children: [_jsxs("div", { class: "relative flex items-center justify-between gap-2 px-3", children: [_jsx("div", { class: "response-tab-list flex gap-1 overflow-x-auto text-xs leading-6", role: "tablist", children: examples.map((ex, i) => (_jsx("button", { type: "button", role: "tab", "aria-selected": i === 0 ? "true" : "false", class: `response-tab group relative my-1 mb-1.5 flex items-center gap-1.5 whitespace-nowrap font-medium outline-0${i === 0 ? " active" : ""}`, "data-response-index": String(i), children: _jsx("div", { class: "z-10 flex items-center gap-1.5 rounded-lg px-1.5 group-hover:bg-[rgb(var(--color-stone-200)/0.5)] group-hover:text-[rgb(var(--color-primary))] dark:group-hover:bg-[rgb(var(--color-stone-700)/0.7)] dark:group-hover:text-[rgb(var(--color-primary-light))]", children: ex.statusCode }) }, ex.statusCode))) }), _jsx("div", { class: "flex shrink-0 items-center justify-end gap-1.5", children: _jsx(CopyButton, {}) })] }), examples.map((ex, i) => (_jsx("div", { class: `response-panel${i === 0 ? " active" : ""}`, role: "tabpanel", "data-response-panel": String(i), children: _jsx("div", { class: "relative w-full px-4 py-3.5 text-sm leading-6 bg-[rgb(var(--color-code-block-light))] dark:bg-[rgb(var(--color-code-block-dark))] overflow-x-auto", style: "font-variant-ligatures: none", children: _jsx("div", { class: "font-mono whitespace-pre text-xs leading-[1.35rem]", dangerouslySetInnerHTML: { __html: ex.html } }) }) }, ex.statusCode)))] }));
}
function renderResponseType(r) {
    const schema = getResponseSchema(r);
    if (!schema)
        return null;
    return _jsx(SchemaDatatype, { schema: schema });
}
function getResponseSchema(r) {
    if (!r.content)
        return null;
    const firstMedia = Object.values(r.content)[0];
    return firstMedia?.schema ?? null;
}
