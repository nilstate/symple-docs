interface LogoProps {
    href: string;
    logo?: {
        light?: string;
        dark?: string;
    };
    name?: string;
    height?: string;
}
/**
 * Shared logo rendering — light/dark images with text fallback.
 * Used by both Header and mobile drawer.
 */
export declare function Logo({ href, logo, name, height }: LogoProps): import("preact").JSX.Element;
export {};
//# sourceMappingURL=Logo.d.ts.map