/**
 * Language icons for the code sample switcher.
 *
 * DevIcon plain variants (MIT). Designed for monochrome rendering at small sizes.
 * https://devicon.dev/
 */
/**
 * Render a language icon as HTML.
 * curl uses a stroked Heroicon. All others use DevIcon via data-lang + CSS mask.
 */
export declare function langIcon(lang: string): string;
/**
 * Generate CSS rules for all language icon masks.
 * Call once at render time and inject into a <style> tag.
 */
export declare function langIconCSS(): string;
//# sourceMappingURL=lang-icons.d.ts.map